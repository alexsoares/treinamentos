<div id="conteudo1">
<fieldset><legend><font size=6>Cadastro de Cursos</font></legend>
<table border="2">
  <td>
   <% form_for(@curso) do |f| %>
   <%= f.error_messages %>
     <table>
       <br/>
        <tr>
          <td><b><%= f.label :Nome %></b></td>
          <td><%= f.text_field :Nome %></td>
        </tr>
        <tr>
          <td><b><%= f.label :Ministrante %></b></td>
          <td><%= f.text_field :Ministrante %></td>
        </tr>
        <tr>
          <td><b>Carga Horária</b> </td>
          <td><%= f.time_select :CargaHoraria, :discard_minutes => true %>horas   --
              <%= f.label  :Ano %>
              <%= f.date_select :Ano, :order => [:year] %></td>
         </tr>
         <tr>
           <td valign="top"><b><%= f.label :Observações %></b></td>
           <td><%= f.text_area(:Obs, :cols =>"33", :rows => "5") %></td>
         </tr>
         <tr>
            <td colspan="2" align="right"><%= f.submit "Salvar" %></td>
         </tr>
     </table>
    <% end %>
   <td VALIGN=TOP>

        <font size="4"><u>OBSERVAÇÕES:</u></font> <br /> <br />
          1. UTILIZAR  SOMENTE LETRAS MAIÚSCULAS<br />
          2. DIGITAR SEMPRE DADOS COMPLETOS<br/>
          3. NÃO UTILIZAR ACENTUAÇÃO, TIL E CEDILHA<br />
          4. NÃO UTILIZAR ABREVIAÇÕES<br />
          5. NO CAMPO OBSERVAÇÕES INFORMAR: <br/>
             > DATA DE REALIZAÇÃO DO CURSO<br/>
             > LOCAL DE REALIZAÇÃO DO CURSO<br/>
             > QUEM PROVOME O CURSO<br/>
             > INFORMAÇÕES IMPORTANTES DO CURSO
    
    </td>

   </td>
</table>
</fieldset>
  <%  if ($var==1) %>
     <%= button_to 'Voltar', voltar_path %>
 <% else %>
     <%= button_to 'Voltar', new_gerenciar_path %>
 <%end%>
</div>