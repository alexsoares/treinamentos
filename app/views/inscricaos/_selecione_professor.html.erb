<%= collection_select :inscricao,:participante_id, @participantes, :id, :nome, :include_blank => ' -- Selecione -- ' %>
<%= observe_field 'inscricao_participante_id',
                   :with => :inscricao_participante_id,
                   :update => 'teste',
                   :url => { :action => 'sel_participa' } %>